/*!
 * Placeholder lightweight subset of jQuery features to support simple selectors and event handling.
 * For production, include the official jQuery build from https://code.jquery.com/.
 */

(function (global) {
  function $(selector, context) {
    const nodes = (context || document).querySelectorAll(selector);
    return new $.fn.init(nodes);
  }

  $.fn = $.prototype = {
    constructor: $,
    length: 0,

    init(nodes) {
      Array.prototype.push.apply(this, nodes);
      return this;
    },

    each(callback) {
      Array.prototype.forEach.call(this, function (el, index) {
        callback.call(el, index, el);
      });
      return this;
    },

    addClass(className) {
      return this.each(function () {
        this.classList.add(className);
      });
    },

    removeClass(className) {
      return this.each(function () {
        this.classList.remove(className);
      });
    },

    toggleClass(className, force) {
      return this.each(function () {
        this.classList.toggle(className, force);
      });
    },

    on(eventName, handler) {
      return this.each(function () {
        this.addEventListener(eventName, handler);
      });
    },
  };

  $.fn.init.prototype = $.fn;
  global.$ = global.jQuery = $;
})(window);

